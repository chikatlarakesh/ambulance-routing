# Architecture Reference

## 1 — System Overview

```mermaid
graph TD
    Client["HTTP Client\n(browser / dispatcher app)"]
    API["FastAPI Application\napi/main.py"]
    Graph["Graph Engine\ncore/graph.py"]
    Routing["Routing Algorithms\ncore/routing.py"]
    Simulator["Simulator\ncore/simulator.py"]
    Config["Config\ncore/config.py"]
    Logging["Logging\ncore/logging_config.py"]

    Client -->|POST /api/v1/route_ambulance| API
    Client -->|POST /api/v1/traffic_snapshot| API
    Client -->|POST /api/v1/reroute_check| API
    Client -->|POST /api/v1/update_position| API
    Client -->|GET  /api/v1/debug/*| API

    API --> Graph
    API --> Routing
    Graph --> Config
    Routing --> Graph
    Simulator --> Graph
    Simulator --> Routing
    API --> Logging
    Simulator --> Logging
```

---

## 2 — Folder Structure

```
ambulance-routing/
|
+-- api/
|   +-- main.py             # FastAPI app, middleware, endpoints, route logic
|   +-- schemas.py          # All Pydantic request/response models + validators
|   +-- __init__.py
|
+-- core/
|   +-- graph.py            # Graph, nodes, edges, travel-time, traffic updates
|   +-- routing.py          # Dijkstra, A*, time-dependent helpers
|   +-- simulator.py        # Virtual-time simulation engine
|   +-- config.py           # All tunable constants (env-var overridable)
|   +-- logging_config.py   # Structured logging setup
|   +-- __init__.py
|
+-- benchmarks/
|   +-- benchmark.py        # Algorithm micro-benchmark (Dijkstra vs A*)
|   +-- load_test.py        # HTTP stress test (100/500/1000 concurrent)
|
+-- tests/
|   +-- test_api.py         # FastAPI endpoint tests (87 tests)
|   +-- test_graph.py       # Graph unit tests
|   +-- test_routing.py     # Routing algorithm tests
|   +-- test_simulator.py   # Simulator unit tests
|
+-- examples/
|   +-- sample_graph.json   # 4-node Bangalore sample
|   +-- city_graph.json     # 50-node Bangalore city graph
|   +-- traffic_snapshot.json
|
+-- docs/
|   +-- architecture.md     # This file
|   +-- benchmark_report.md # Auto-generated by benchmarks/benchmark.py
|   +-- DEMO.md
|
+-- Dockerfile              # Multi-stage production build
+-- docker-compose.yml      # Single-command startup
+-- pyproject.toml          # Black, isort, mypy, pytest config
+-- .flake8                 # Flake8 lint config
+-- .github/workflows/      # CI: lint + test on Python 3.10 & 3.11
```

---

## 3 — Route Request Flow

```mermaid
sequenceDiagram
    participant C as Client
    participant A as API (main.py)
    participant G as Graph
    participant R as Routing

    C->>A: POST /api/v1/route_ambulance {lat, lon, dst, depart_time}
    A->>G: nearest_node(current_location)
    A->>G: nearest_node(destination)
    A->>R: time_dependent_dijkstra(graph, src, dst, depart_dt)
    R->>G: edge_travel_time(eid, t) [per relaxation]
    R-->>A: (arrival_dt, path, per_segment_times)
    A->>A: _store_route(ambulance_id, ...)
    A-->>C: RouteResponse {path, eta, steps}
```

---

## 4 — Traffic Update + Auto-Reroute Flow

```mermaid
sequenceDiagram
    participant C as Client
    participant A as API
    participant G as Graph
    participant R as Routing

    C->>A: POST /api/v1/traffic_snapshot {edge_updates}
    loop For each EdgeUpdate
        A->>G: apply_edge_update(multiplier | absolute_time)
    end
    loop For each active ambulance
        A->>A: _recalculate_eta(ambulance_id, now)
        A->>A: _compute_remaining_path_cost(old_path)
        A->>R: time_dependent_dijkstra(from_current_node)
        alt time_saved >= REROUTE_THRESHOLD_SEC (120s)
            A->>A: update route state (REROUTED)
            A->>A: append reroute_event
        else below threshold
            A->>A: refresh remaining_seconds only
        end
    end
    A-->>C: {applied, errors, auto_reroutes}
```

---

## 5 — Simulation Flow

```mermaid
sequenceDiagram
    participant U as User / Test
    participant S as SimulationEngine
    participant G as Graph
    participant R as Routing

    U->>S: run(ambulance_id, start, end, depart_dt, injections)
    S->>R: time_dependent_dijkstra(initial route)
    loop For each segment
        S->>S: advance virtual clock to seg_end
        opt traffic injection fires this segment
            S->>G: apply_edge_update(...)
            S->>R: time_dependent_dijkstra(from current node)
            alt saved >= threshold
                S->>S: update path + ETA (REROUTE event)
            else
                S->>S: log not beneficial
            end
        end
    end
    S-->>U: SimResult {events, path, total_seconds, reroutes}
```

---

## 6 — Class Relationships

```mermaid
classDiagram
    class Graph {
        +nodes: Dict[int, NodeData]
        +edges: Dict[int, EdgeData]
        +adj: Dict[int, List[Tuple]]
        +add_node(id, lat, lon, name)
        +add_edge(u, v, base_time, dist, ...) int
        +nearest_node(latlon) int
        +edge_travel_time(eid, depart_t) float
        +apply_edge_update(EdgeUpdate)
        +reset_edge_overrides(edge_id?)
        +load_from_file(path)
        +graph_to_dict() dict
    }

    class EdgeUpdate {
        +edge_id: int
        +multiplier: Optional[float]
        +absolute_time: Optional[float]
    }

    class SimulationEngine {
        +graph: Graph
        +threshold: float
        +run(ambulance_id, start, end, ...) SimResult
    }

    class SimResult {
        +ambulance_id: str
        +departed: datetime
        +arrived: datetime
        +total_seconds: float
        +path: List[int]
        +reroutes: int
        +events: List[SimEvent]
        +log() str
    }

    class SimEvent {
        +sim_time: datetime
        +kind: str
        +message: str
        +data: dict
    }

    class TrafficInjection {
        +trigger_at: datetime
        +edge_updates: List[EdgeUpdate]
        +label: str
    }

    Graph --> EdgeUpdate : accepts
    SimulationEngine --> Graph : reads/writes
    SimulationEngine --> SimResult : produces
    SimResult --> SimEvent : contains
    SimulationEngine --> TrafficInjection : consumes
```

---

## 7 — API Route Map

```mermaid
graph LR
    subgraph "v1 Routes (canonical)"
        R1["POST /api/v1/route_ambulance"]
        R2["POST /api/v1/route_ambulance_astar"]
        R3["POST /api/v1/traffic_snapshot"]
        R4["POST /api/v1/reroute_check"]
        R5["POST /api/v1/update_position"]
        D1["GET /api/v1/debug/edges"]
        D2["GET /api/v1/debug/active_routes"]
        D3["POST /api/v1/debug/reset_overrides"]
        D4["GET /api/v1/debug/reroute_events"]
    end

    subgraph "Legacy aliases (backward compat)"
        L1["POST /route_ambulance"]
        L2["POST /route_ambulance_astar"]
        L3["POST /traffic_snapshot"]
        L4["POST /reroute_check"]
        L5["POST /update_position"]
    end

    L1 -.->|delegates to| R1
    L2 -.->|delegates to| R2
    L3 -.->|delegates to| R3
    L4 -.->|delegates to| R4
    L5 -.->|delegates to| R5
```

---

## 8 — Edge Travel Time Priority

```
absolute_time override (set via /traffic_snapshot)
        |
        v  (if not set)
matching time_bucket (seconds-of-day in range) * multiplier
        |
        v  (if no bucket matches)
base_time * multiplier
```

---

## 9 — Reroute Decision Logic

```
old_remaining = _compute_remaining_path_cost(current_graph, old_path, from_node, now)
new_remaining = Dijkstra(from_node, dest, now).arrival - now

time_saved = old_remaining - new_remaining

if time_saved >= REROUTE_THRESHOLD_SEC (default 120s):
    REROUTE

elif any of next SLOWDOWN_LOOKAHEAD (3) edges has current_travel_time / baseline > SLOWDOWN_RATIO (1.5):
    REROUTE
```

The key insight: `old_remaining` is computed from the **current graph costs** (not the stale stored ETA),
so reroute decisions are always based on actual current conditions.
